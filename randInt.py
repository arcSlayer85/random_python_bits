import random
# create an index of numbers
index = [6,5,4,3,2,1];
# generate a random number from 0, 5
rand = random.randint(0, 5);
# use the random number to choose a number to print from the index
print (index[rand]);




